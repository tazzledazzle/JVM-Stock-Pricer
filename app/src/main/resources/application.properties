server.port=8080
# Spring MVC handles each HTTP request on a virtual thread (Spring Boot 3.2+).
# Our own provider polling/aggregation threads are created explicitly via
# Executors.newVirtualThreadPerTaskExecutor() in Aggregator.java, independent
# of this setting -- this just covers the request-handling threads.
spring.threads.virtual.enabled=true